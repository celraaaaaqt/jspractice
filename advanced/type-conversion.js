// ? typeof tells you the type of a value.

// todo type conversion exercises.

const price = "1500";

const priceConvert = Number(price) + 500;
console.log(priceConvert);
console.log(typeof priceConvert);

const age = 21;

const ageConvert = String(age);
console.log(typeof ageConvert);

